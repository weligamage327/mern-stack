import React from "react";
import Header from "../../components/Header";
import Footer from "../../components/Footer";

const UserProfile = () => {
  // Your User Profile page logic

  return (
    <div>
      <Header />
      {/* User Profile page content */}
      <h1>User Profile Page</h1>
      {/* Display user information, settings, saved trips, bookings, etc. */}
      {/* Other components or content specific to the User Profile page */}
      <Footer />
    </div>
  );
};

export default UserProfile;
